# Client-exposed build-time vars (Astro reads PUBLIC_* and inlines them).
# Copy to `.env` and fill in.

# Cloudflare Turnstile site key (safe to expose). Leave unset to hide the widget.
# Always-pass test site key: 1x00000000000000000000AA
PUBLIC_TURNSTILE_SITE_KEY="1x00000000000000000000AA"
